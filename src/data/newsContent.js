// Future CMS/API items should follow this shape:
// { id, slug, title, date, category, summary, image, body, featured }
export const newsItems = [];

// Future event items should follow this shape:
// { id, title, date, summary, location }
export const upcomingEvents = [];

export const newsCategories = ["News", "Announcements", "Events"];
